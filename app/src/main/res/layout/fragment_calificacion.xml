<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="24dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp">
        <ImageButton
            android:id="@+id/btnBackCalifi"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_arrow_back" />
        <TextView
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="Calificación" android:textSize="20sp" android:textStyle="bold" />
    </RelativeLayout>



    <TextView
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:gravity="center"
        android:text="¿Cómo calificarías la experiencia\ny el servicio?"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Rating Bar Personalizado con estrellas -->
    <RatingBar
        android:id="@+id/ratingBar"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:numStars="1"
        android:progressTint="#FFD700"
        android:rating="5" />

    <TextView
        android:id="@+id/txtRatingLabel"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="5 - Good"
        android:textSize="16sp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGuardarCalif"
        android:layout_width="match_parent" android:layout_height="56dp"
        android:layout_marginTop="60dp"
        android:backgroundTint="#2E3B8E"
        android:text="Guardar calificación"
        app:cornerRadius="28dp"
        app:icon="@drawable/ic_check_circle_green" />

</LinearLayout>